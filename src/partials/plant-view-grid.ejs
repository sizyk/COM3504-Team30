<div
  id="plant-grid"
  class="grid grid-cols-1 gap-10 md:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4"
>
    <!-- prettier-ignore -->
    <% plants?.forEach((p) => { %>
      <%- include('plant-card.ejs', {plant: p}) %>
    <% }) %>
</div>