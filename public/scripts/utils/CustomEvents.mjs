/**
 * Custom events, to prevent accidental mistyping when dispatching
 */

export const plantAddEvent = new Event('plant-added');
export const IDBOpenEvent = new Event('idb-open');
export const leafletInvalidateEvent = new Event('invalidate-leaflet');
